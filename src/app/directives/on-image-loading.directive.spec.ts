import { OnImageLoadingDirective } from './on-image-loading.directive';

describe('OnImageLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new OnImageLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
